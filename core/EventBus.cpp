#include "../../include/core/EventBus.hpp"

// EventBus is mostly template-based, but we provide a compilation unit
// to ensure the event structs have symbols defined.
// The structs are trivial aggregates, no extra definitions needed.
